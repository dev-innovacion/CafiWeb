@model Rivka.Form.Field.FieldBase
@*@model dynamic*@

@Html.EditorForModel(Model)

<script>
    setTimeout(function () {
        console.log(jQuery("#listId").val());
        jQuery("#CustomList").val(jQuery("#listId").val());
    }, 400);
    
</script>